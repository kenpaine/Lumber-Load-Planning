# Node
node_modules/
*.log

# Synced web app (regenerated from repo-root lumber_loader.html by scripts/sync-www.mjs)
www/index.html

# Capacitor-copied bundle inside the iOS project (regenerated by `cap sync`)
ios/App/App/public/

# CocoaPods (installed on the Mac by `cap sync` / `pod install`)
ios/App/Pods/

# macOS / Xcode cruft
.DS_Store
ios/App/build/
xcuserdata/
*.xcuserstate
