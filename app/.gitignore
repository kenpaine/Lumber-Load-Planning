# Node
node_modules/
*.log

# Synced web assets (regenerated from repo root by scripts/sync-www.mjs)
www/tally.html
www/planner.html

# Capacitor-copied bundle inside the iOS project (regenerated by `cap sync`)
ios/App/App/public/

# CocoaPods (installed on the Mac by `cap sync` / `pod install`)
ios/App/Pods/

# macOS / Xcode cruft
.DS_Store
ios/App/build/
xcuserdata/
*.xcuserstate
