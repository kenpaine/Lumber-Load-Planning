{
  "name": "lumber-load-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "iOS app wrapper for the combined Lumber Loader web app (Tally Recommender + Layout Planner with built-in tally->loader handoff).",
  "type": "module",
  "scripts": {
    "sync-www": "node scripts/sync-www.mjs",
    "build": "npm run sync-www",
    "cap:add-ios": "npm run sync-www && cap add ios",
    "cap:sync": "npm run sync-www && cap sync ios",
    "cap:open": "cap open ios",
    "assets": "capacitor-assets generate --ios"
  },
  "author": "Ken Paine",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.1"
  }
}
